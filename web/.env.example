# ---- AI brain (FREE) ----
# Get a free key at https://console.groq.com (no card required).
AI_API_KEY=gsk_your_free_groq_key
# Defaults target Groq's free tier. Any OpenAI-compatible endpoint works.
AI_BASE_URL=https://api.groq.com/openai/v1
AI_MODEL=llama-3.3-70b-versatile

# ---- Contract (fill after free testnet deploy) ----
NEXT_PUBLIC_REGISTRY_ADDRESS_TESTNET=0x
NEXT_PUBLIC_REGISTRY_ADDRESS_MAINNET=0x

# ---- OKX DEX Aggregator API (OPTIONAL) ----
# Leave blank for $0 — swaps use the free deep-link path. Fill only if you get
# DEX API access and want prepared/one-click swaps. Server-side only.
OKX_API_KEY=
OKX_API_SECRET=
OKX_API_PASSPHRASE=
OKX_API_PROJECT=
